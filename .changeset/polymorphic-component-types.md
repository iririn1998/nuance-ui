---
"neumorph-ui": patch
---

Support Mantine-aligned polymorphic component types (`NuPaper`, `NuButton`, `NuActionIcon`, `NuBadge`, `NuAvatar`, `NuCard`, `NuCardSection`, `NuAppShellSection`) with `createPolymorphicComponent`, enabling proper inference of `component`, element-specific props, and `ref` types.
